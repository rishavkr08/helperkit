const Header = () => {
  return(
    <div className="flex h-12 w-full bg-gradient-to-r from-indigo-500 to-sky-500 text-white items-center px-4">
      Project is built with: React + Vite + TS + SWC
    </div>
  )
}

export default Header;